import { minecratProtocolWrapper } from './minecrat-protocol-wrapper';

describe('minecratProtocolWrapper', () => {
  it('should work', () => {
    expect(minecratProtocolWrapper()).toEqual('minecrat-protocol-wrapper');
  });
});
